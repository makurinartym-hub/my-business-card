#ifndef STR_H
#define STR_H
#include <iostream>
#include <string>
#include <vector>

bool acronim(std::string s, std::vector<string> words);
bool pallindrom(std::string s);
int maxscore(std::vector<string> words, std::vector<string> letters, std::vector<int> score(26));

#endif